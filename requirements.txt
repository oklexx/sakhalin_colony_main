# Sakhalin Colony — Python-зависимости (Python 3.10+, CUDA GPU)
# Сборка C++-среды: build_pyext.bat (Windows) или `make build` (CMake + C++17)

# Обучение
torch>=2.13.0
numpy>=1.26.0
gymnasium>=0.29.0
stable-baselines3>=2.1.0
tensorboard>=2.14.0

# Сборка colony_cpp.pyd
pybind11>=2.12.0
cmake>=3.20  # build_pyext.bat uses `python -m cmake` to build colony_cpp.pyd

# UI (дашборд train_ui2)
PySide6>=6.6.0
pyqtgraph>=0.13.0

# Тесты (pytest.ini, tests/)
pytest>=8.0.0
