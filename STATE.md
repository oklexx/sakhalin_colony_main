# STATE — текущее состояние проекта

> Файл обновляется при смене задач. Завершённые пункты переносятся в
> [CHANGELOG.md](CHANGELOG.md). Последнее обновление: 2026-09-19.

## Статус

Проект стабильно работает в режиме обучения: PPO + C++-среда (49 действий,
obs v2 = 299), канонический профиль наград — `configs/reward_v4.json`
(терминальный бонус за выживание, дисциплина кредитов, предналоговое давление).

Синхронизация дефолтов закрыта (2026-09-19): C++ `RewardConfig` и дефолт
obs приведены к канону (v4 / obs v2) на всех границах; полный `pytest`
зелёный (318 passed), C++-пробы `tests/cpp` — 0 fail. Регрессии плана
ревью — `tests/test_review_plan_regressions.py`. Прогон под Linux:
`cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build`
(нужны pybind11, numpy, pytest; расширение — `python/colony_cpp*.so`).

## Актуальные баги / проблемы (по приоритету)

Пункты 1–6 предыдущей редакции закрыты 2026-09-19 и перенесены в
[CHANGELOG.md](CHANGELOG.md); разбор с измерениями —
[docs/REMAINING_WORK_2026_09.md](docs/REMAINING_WORK_2026_09.md).

1. **Дорога не наводится на воду по диагонали** (найдено при закрытии P0-1).
   Легальность маски WaterChannel корректна (457/457 состояний, зонд
   `tests/cpp/water_mask_check.cpp`), но на seed 100 жадный выбор направления
   даёт `ROAD_W` и «проезжает» цель: дистанция 14.42 → 7.81 (минимум, шаг 11)
   → 19.42 (шаг 40), маска не открылась за 400 шагов. `find_lot_dir` максимизирует смещение по
   оси и не имеет понятия цели; 4 направленных действия не покрывают диагональ.
   Следующий шаг — оценивать кандидата по уменьшению дистанции до ближайшей
   воды (или 8 направлений); регресс-зонд уже есть.
2. **Цена WaterChannel 18 310** против стартового бюджета 82 000 и постоянных
   расходов. В агрегате по 8 сидам (457 состояний) маска закрыта деньгами в
   240 состояниях и отсутствием легальной клетки в 210, курикулумом — в 0.
   Нужен аудит баланса (`src/data.cpp`).
3. **P2-7: распил `src/env.cpp` (2252 строки)** — отдельным PR, порядок выноса
   по риску в плане. `lot_ok` уже тонкая обёртка, `action_mask` без логики
   легальности — выносить стало проще.
4. **Обучение на v4 не прогнано.** Верификация награды сделана фиксированными
   политиками (зонд `reward_v4_longrun`); прогон PPO на уменьшенных
   `configs/exp_*_n64.json` требует GPU, которого в песочнице нет.
5. **CI не прогнан ни разу.** `.github/workflows/ci.yml` добавлен; сборка
   `colony_cpp` и Qt-тесты локально непроверяемы (нет заголовков CPython и
   системных библиотек Qt) — первый зелёный прогон в Actions ещё впереди.

## Текущие задачи

- [x] Диагностика маски WaterChannel (P0-1) — легальность корректна, остаток
      переквалифицирован в задачу наведения (см. п. 1).
- [x] Верификация `reward_v4` на длинном прогоне (P0-2) — v4 ломает
      road-spam-аргмакс; попутно исправлен гибрид в `configs/reward_v3.json`.
- [x] Уменьшенные exp-конфиги под 12–24 ГБ (P0-3).
- [x] README: убраны ссылки на отсутствующие файлы, исправлены размеры obs и
      число полей наград (P1-4).
- [x] `test_ui2_smoke.py` починен, `--ignore` из `pytest.ini` убран (P1-5).
- [x] CI в GitHub Actions + `scripts/cpp_checks.sh` (P1-6).
- [x] P2-8/9/10/11: депрекейт `set_minimap_radius`, road-shaping в
      `RewardConfig`, `lot_ok` → `can_build_at`, кеш в `action_mask` с бенчем.
- [ ] Прогон обучения на `reward_v4.json` против базы v3 (нужен GPU).
- [ ] Первый зелёный прогон CI.
- [ ] P2-7: распил `src/env.cpp` (отдельный PR).
- [ ] Наведение дороги на воду (п. 1) + аудит цены WaterChannel (п. 2).

## Известные ограничения (не баги)

- `survival_coeff`/`goal_survival_coeff` v4: деньги сами по себе дают награду
  только через терминальный бонус — это осознанное решение (P0-1 диагностики).
- Приоритеты ресурсов (`curriculum_resources`) действуют только на реально
  добываемые ресурсы; `w_food = w_gold = 0` всегда по построению (нет
  зданий-потребителей).
- `loop_detector` по умолчанию ВЫКЛЮЧЕН (`loop_detection_enabled=False`).
- Eval — детерминированный argmax с замороженной нормализацией; это не та же
  политика, что при обучении (сэмплирование).
